---
title: "在Yarn集群上跑spark wordcount任务"
date: 2022-01-05T16:56:00+08:00
draft: false
tags: ["大数据"]
categories: ["大数据"]
original_url: https://www.cnblogs.com/gradyblog/p/15767793.html
---

1. 准备的测试数据文件hello.txt

```text
hello scala
    hello world
    nihao hello
    i am scala
    this is spark demo
    gan jiu wan le
```

  2. 将文件上传到hdfs中

```text
#创建hdfs测试目录
    hdfs dfs -mkdir /user/spark/input/
    #上传本地文件hello.txt到hdfs
     hdfs dfs -put ./hello.txt /user/spark/input/
```

  3. 代码（改为读取hdfs上的数据，并写入hdfs）

```java
package org.example
    
    import org.apache.spark.{SparkConf, SparkContext}
    
    /**
     * spark-submit --master yarn --class org.example.SparkWordCountYarn /tmp/test/sparkwordcount2-1.0-SNAPSHOT.jar hdfs://hadoop1:8020/user/spark/input/hello.txt hdfs://hadoop1:8020/user/spark/output/helloOutput
     */
    object SparkWordCountYarn {
    
      def main(args: Array[String]): Unit = {
        val conf = new SparkConf()
          .setAppName("WordCount")
          .setMaster("yarn")
    
        val srcFile = args(0)
        val outPutFile = args(1)
        val sc = new SparkContext(conf)
        val data = sc.textFile(srcFile)
        data.flatMap(_.split(" "))
          .map((_, 1))
          .reduceByKey(_+_)
          .saveAsTextFile(outPutFile)
      }
    }
```

  4. 执行提交spark人物命令

```text
spark-submit --master yarn --class org.example.SparkWordCountYarn /tmp/test/sparkwordcount2-1.0-SNAPSHOT.jar hdfs://hadoop1:8020/user/spark/input/hello.txt hdfs://hadoop1:8020/user/spark/output/helloOutput
```

  5. 执行结果  
![image](https://img2020.cnblogs.com/blog/1267154/202201/1267154-20220105165537472-1289343523.png)
